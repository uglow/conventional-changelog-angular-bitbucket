<a name="{{version}}"></a>
{{#if isPatch~}}
  ##
{{~else~}}
  #
{{~/if}} {{#if @root.linkCompare~}}
  [{{version}}](
  {{~#if @root.repository~}}
    {{~#if @root.host}}
      {{~@root.host}}/
    {{~/if}}
    {{~#if @root.owner~}}
      {{~@root.owner}}/
    {{~/if~}}
    {{~@root.repository}}/
  {{~else}}
    {{~@root.repoUrl}}/
  {{~/if~}}
  compare/{{currentTag}}%0D{{previousTag}}#diff)
{{~else}}
  {{~version}}
{{~/if}}
{{~#if title}} "{{title}}"
{{~/if}}
{{~#if date}} ({{date}})
{{/if}}
